<%= form_for(@mail) do |f| %>
  <% if @mail.errors.any? %>
    <div id="error_explanation" class="alert alert-block">
      <button type="button" class="close" data-dismiss="alert">&times;</button>
      <h4><%= pluralize(@mail.errors.count, "error") %> prohibited this mail from being saved:</h4>
      <br />
      <ul>
      <% @mail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row-fluid">
    <div class="field">
      <%= f.label :address %>
      <%= f.text_field :address %>
    </div>
  </div>
    <div class="actions">
      <%= f.submit :class => 'btn btn-primary' %>
    </div>
<% end %>
