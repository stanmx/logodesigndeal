<div class="page-header">
    <h2>Editing email</h2>
</div>

<%= render 'form' %>

<div class="more_actions">
	<%= link_to 'Show', @email, :class => 'btn' %>
	<%= link_to 'Back', emails_path, :class => 'btn' %>
</div>